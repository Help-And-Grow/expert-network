{
  "name": "hg-tcb-proxy",
  "version": "0.1.0",
  "description": "Tencent CloudBase HTTP-trigger function bridging WeChat Mini Program traffic to the GCP Cloud Run origin.",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "deploy:cn": "tcb framework deploy -c cloudbaserc.cn.json",
    "deploy:intl": "tcb framework deploy -c cloudbaserc.intl.json",
    "logs": "tcb fn log hg-proxy"
  }
}
