export default definePageConfig({
  navigationBarTitleText: "实时咨询",
  // The native <live-pusher> + <live-player> components handle their own
  // landscape orientation; default page orientation stays portrait.
});
